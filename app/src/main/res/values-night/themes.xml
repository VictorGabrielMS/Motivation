<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Motivation" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="primary">@color/blue</item>
        <item name="primaryVariant">@color/light_blue</item>
        <item name="onPrimary">@color/dark_blue</item>
        <!-- Secondary brand color. -->
        <item name="secondary">@color/pink</item>
        <item name="secondaryVariant">@color/light_pink</item>
        <item name="onSecondary">@color/dark_pink</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/onPrimary</item>
        <!-- Customize your theme here. -->
    </style>
</resources>